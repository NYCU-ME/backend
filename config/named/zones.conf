zone "nycu-dev.me" {
    type master;
    file "/var/named/nycu.me.db";

    allow-update { !{ !localhost; any; }; key "ddnskey"; };
    allow-transfer { localhost; };

    key-directory "/etc/bind/keys";
    # auto-dnssec maintain;
    # inline-signing yes;
};
